<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapper.BrowsedMapper">

    <resultMap id="BaseResultMap" type="com.example.bean.Browsed">
        <result column="id" jdbcType="INTEGER" property="id" />
        <result column="name" jdbcType="VARCHAR" property="name" />
    </resultMap>

    <select id="Select" resultType="com.example.bean.Browsed">
        select * from browsed where userID = #{id}
    </select>

    <select id="Sel" resultType="com.example.bean.Job">
        select * from job where jobID = #{jobID}
    </select>

    <insert id="Insert" parameterType="com.example.bean.Browsed" useGeneratedKeys="true" keyProperty="id">
        insert into browsed(userID)
        values(#{id})
    </insert>

    <update id="Update" parameterType="com.example.bean.Browsed" useGeneratedKeys="true" keyProperty="id">
        update browsed
        set
        where userID=#{id}
    </update>

</mapper>